const getCart = () => {
    

    // check if user exist
    // if yes fetch the cart using userIdentifier
    // else check for session_id in cookies 
    // if found fetch using that 
    
    // else return null
}