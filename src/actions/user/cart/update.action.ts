const updateCart = () => {
  // check if user exist
  // if yes fetch the cart using userIdentifier and if cart found then update it else create a new and link it to user
  // else check for session_id in cookies
  // if found fetch using that
  // else create a new cart
};
